export const config = {
    // Maximal number of characters a Comment can have
    COMMENT_MAX_LENGTH: 900,

    // Maximal number of images one can use for a Submission
    IMAGE_MAX_NUMBER: 10,

    //Maximal number of characters a Submission description can have
    SUBMISSION_DESCRIPTION_MAX_LENGTH: 6000,

    //Maximal number of characters a Submission name can have
    SUBMISSION_NAME_MAX_LENGTH: 60,

    // Maximal number of characters a Profile name can have
    PROFILE_NAME_MAX_LENGTH: 30,
}